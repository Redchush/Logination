package by.epam.like_it.dao.criteria;


import by.epam.like_it.dao.ConcreteCriteria;
import by.epam.like_it.model.Role;

public enum RoleCriteria implements ConcreteCriteria<Role> {

    USERS$BANNED, USERS$CREATED_DATE, USERS$EMAIL, USERS$FIRST_NAME, USERS$ID,
    USERS$LAST_NAME, USERS$LOGIN, USERS$PASSWORD, USERS$ROLE_ID, USERS$UPDATED_DATE,

    _$ID, _$NAME,
    LIMIT
}
